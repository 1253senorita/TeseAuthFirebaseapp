<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.salsample.teseauthfirebaseapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TeseAuthFirebaseApp">

        <!-- 스플래시 액티비티: Android 11 이하: Theme.App.SplashLegacy, Android 12+일 경우에는 Theme.App.Starting 사용 -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.App.SplashLegacy">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 로그인/등록 화면용 액티비티 -->
        <activity android:name=".AuthActivity" />

        <!-- 메인 콘텐츠 액티비티 -->
        <activity android:name=".MainActivity" />

        <!-- 다른 액티비티들 추가 가능 -->
    </application>

</manifest>